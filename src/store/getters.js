const getters = {
  formModels: state => state.formModels.models,
  widget: state => state.widget,
  membersList: state => state.formModels.membersList,
  insuranceForm: state => state.formModels.insuranceForm
}
export default getters
